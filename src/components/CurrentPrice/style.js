import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    headerPrice: {
        width:"100%",
        height:"auto",
        alignItems:"center",
        marginBottom:20,
    },
    currentPrice: {
        color: 'rgb(13, 127, 13)',
        fontSize:32,
        fontWeight: "bold",
        paddingTop:20,
    },
    textTitle: {
        color: "white",
        fontSize:16,
        fontWeight:"bold",
    }
});

export default styles